"""
All modules relating to the docker autotest sub-framework are in this namespace

Through subclassing ``dockertest.subtest.Subtest()``, `subtest modules`_,
also have access to this namespace.  It is available using the standard
python dotted import syntax.  For example, ``from dockertest import output``.
"""
